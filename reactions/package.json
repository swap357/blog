{
  "name": "autoscaler-reactions",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787 --var ALLOWED_ORIGINS:http://127.0.0.1:1313,http://localhost:1313",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
